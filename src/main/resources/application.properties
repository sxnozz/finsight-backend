spring.application.name=finsight-backend

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/finsight}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASS:12345}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update

api.security.token.secret=${JWT_SECRET}

gemini.api.key=${GEMINI_API_KEY:cole_sua_chave_do_google_aqui}

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USER:seu_email_aqui@gmail.com}
spring.mail.password=${MAIL_PASS:sua_senha_de_app_aqui}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

finsight.email.dev-mode=${EMAIL_DEV_MODE:false}

app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173}

finsight.demo.email=demo@finsight.com
finsight.demo.password=demo123